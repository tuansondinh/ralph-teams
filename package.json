{
  "name": "ralph-teams",
  "version": "1.0.14",
  "description": "CLI tool for Ralph Teams",
  "bin": {
    "ralph-teams": "dist/index.js",
    "rjq": "dist/json-tool.js"
  },
  "files": [
    "dist",
    "prompts",
    ".claude",
    ".github/agents",
    ".codex",
    "ralph.sh",
    "prd.json.example"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc && chmod +x dist/index.js dist/json-tool.js",
    "test": "node --import tsx --test $(find ./test -type f -name '*.test.ts' | sort)",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "js-yaml": "^4.1.1"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
