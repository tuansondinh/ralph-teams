timeouts:
  epicTimeout: 3600
  idleTimeout: 600

execution:
  backend: claude
  parallel: 0
  validatorMaxPushbacks: 1

agents:
  teamLead: haiku
  planner: haiku
  builder: haiku
  validator: haiku
  merger: haiku
