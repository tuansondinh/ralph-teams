workflow:
  preset: balanced

timeouts:
  epicTimeout: 3600
  idleTimeout: 600
  loopTimeout: 18000

execution:
  backend: claude
  parallel: 3
  storyPlanning:
    enabled: false
  storyValidation:
    enabled: false
    maxFixCycles: 1
  epicPlanning:
    enabled: true
  epicValidation:
    enabled: true
    maxFixCycles: 1
  finalValidation:
    enabled: true
    maxFixCycles: 1

agents:
  teamLead: haiku
  storyPlanner: haiku
  epicPlanner: haiku
  builder: haiku
  storyValidator: haiku
  epicValidator: haiku
  finalValidator: haiku
  merger: haiku
